import type { Config } from 'tailwindcss';

// Tailwind v4: custom colors are defined via @theme in app/globals.css.
// This config is kept for plugins and any future JS-side overrides.
const config: Config = {
  plugins: []
};

export default config;
